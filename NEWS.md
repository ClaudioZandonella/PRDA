# PRDA 1.0.2
* Removed explicit `CXX_STD = CXX11` from `Makevars` and `Makevars.win` to comply with modern R standards and ensure future compatibility with dependencies. The package now uses the system's default C++ standard.

# PRDA 1.0.1
* Removed explicit `CXX_STD = CXX11` from `Makevars` to comply with modern R standards and ensure future compatibility with dependencies. The package now uses the system's default C++ standard.
